import { createApp } from 'vue'
import App from './App.vue'
import router from './router/router'


// Resto do código para configurar o servidor, definir rotas, etc.


createApp(App).use(router).mount('#app')
