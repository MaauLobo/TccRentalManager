# Rental Manager

O Rental Manager é um programa de gerenciamento de aluguéis de brinquedos.

## 🎲 Banco de Dados
É necessário rodar o arquivo `database.txt` para criar as tabelas e triggers necessárias no banco de dados.

### 🔙 Backend
Certifique-se de colocar os valores corretos das variáveis dentro do arquivo. Para instalar as dependências do backend, execute o seguinte comando:

```
npm i
```

Para executar a API, navegue até o diretório `api` e execute o seguinte comando:

```
npm run dev
```

## 🖥️ Frontend
Para executar o frontend, navegue até o diretório `web` e execute os seguintes comandos:

```
cd web
npm run dev
```

## ⚙️ Tecnologias
- Vue.js
- Node.js
- MySQL


